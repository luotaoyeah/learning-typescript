/**
 * Listing 1-35. Shorthand Boolean test
 */

function booleanTestExample(val: any) {
  if (val) {
    /*
     * val is not null
     * val is not undefined
     * val is not 0
     * val is not NaN
     * val is not ''
     * val is not false
     *
     */
  }
}

export {};
