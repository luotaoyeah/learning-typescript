/*
 * Modules
 *     Default exports
 */

console.log("\n-------------------------------------------------- 01");
/*
 * 可以使用 export default 指定模块的默认输出，
 * 每个模块最多只能有一个默认输出，也可以没有默认输出
 */
const name: string = "FOO";
export default name;
