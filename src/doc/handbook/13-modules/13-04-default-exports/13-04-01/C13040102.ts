/*
 * Modules
 *     Default exports
 */

console.log("\n-------------------------------------------------- 01");
/*
 * 在语法上，引入 default export 和引入 named export 有些不同，
 * 不需要使用 {}
 * 且引入时的名称可以任意取
 */

import n from "./C13040101";
console.log(n);
