/*
 * Decorators
 *     Introduction
 */

console.log("\n-------------------------------------------------- 01");
/*
 * 装饰器（decorator ）在 ECMA 规范中还处于 stage 2 proposal
 * 在 TS 中也只是一个试验性（experimental）的特性，
 * 因此需要在 tsconfig.json 配置 { "compilerOptions": { "experimentalDecorators": true } }
 * 或者通过命令行参数指定：tsc --experimentalDecorators
 */
export {};
